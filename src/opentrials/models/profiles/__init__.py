"""Concrete, registered model capability profiles.

Each module here declares one model's capability profile as data. Nothing
in this package is imported by ``models.capability``/``models.registry``
(the generic core) -- only the reverse. See ``models.profiles.aciclovir_iv``
for the first, and so far only, registered profile.
"""

"""OpenTrials Studio: a local, researcher-facing GUI for ``opentrials.sdk``.

Studio is a second client of the same public SDK the CLI is built on -- see
``bridge.py``'s module docstring for the one rule that keeps it that way.
"""

from __future__ import annotations
